"""The nine self-awareness components.

Each module is small, single-purpose, and writes to exactly one artifact
path. None of them is required at runtime; the pipeline runs fine with
zero of them active. See concepts/self_awareness_loop.md for the full
design.
"""
