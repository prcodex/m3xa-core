# For Claude Code

Read [`BODY.md`](BODY.md) first. It is the live infrastructure map for this repo, autogenerated from the codebase.

Then read in order: [`AGENTS.md`](AGENTS.md), [`concepts/the_house.md`](concepts/the_house.md), [`concepts/self_awareness_loop.md`](concepts/self_awareness_loop.md), [`LESSONS.md`](LESSONS.md).

This repo is **public + anonymized**. Real source names (banks, expert analysts, podcasts) are replaced with `Bank1`, `Expert1`, etc. — see [`docs/source_naming.md`](docs/source_naming.md). Run `python tools/check_anonymization.py` before every commit; CI blocks any leak.

After any structural change, run `python tools/regenerate_body.py` so BODY.md stays in sync. The pre-commit hook does this automatically.
